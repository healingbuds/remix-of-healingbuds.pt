// Re-export everything from the context for backward compatibility
export { 
  useNewsRegion, 
  regionOptions, 
  NewsRegionProvider 
} from '@/context/NewsRegionContext';

export type { NewsRegion } from '@/context/NewsRegionContext';
